public class main {
    public static void main(String[] args) {
   
    LinkedList myLinkedList = new LinkedList(4); // Create a new linked list with a head node of value 10

    myLinkedList.getHead(); // Get the head of the linked list
    myLinkedList.getTail(); // Get the tail of the linked list
    myLinkedList.getLength(); // Get the length of the linked list
    myLinkedList.printList(); // Print the linked list
    }
}
