

public class main {
    public static void main(String[] args) {
   


        DoublyLinkedList myDoublyLinkedList = new DoublyLinkedList(1); // Create a new doubly linked list with the first node having value 1
        
        myDoublyLinkedList.append(2); // Append a new node with value 2 to the doubly linked list
        myDoublyLinkedList.append(3); // Append a new node with value 3 to the doubly linked list
        myDoublyLinkedList.append(4); // Append a new node with value 4 to the doubly linked list
       // myDoublyLinkedList.append(5); // Append a new node with value 5 to the doubly linked list
        myDoublyLinkedList.printList(); // Print the doubly linked list
        System.out.println("\n"); // Print a new line for better readability
        myDoublyLinkedList.swapPairs(); 
        myDoublyLinkedList.printList(); // Print the doubly linked list after swapping the first and last nodes

        /*     LinkedList myLinkedList = new LinkedList(1); 
    myLinkedList.append(2); 
    myLinkedList.append(3); 
    myLinkedList.append(4);
    myLinkedList.append(5);

    myLinkedList.printList(); // Print the linked list
    
    myLinkedList.reverseBetween(2,3); // Reverse the linked list between indices 2 and 3
    myLinkedList.printList(); // Print the linked list after reversal
 */
    /* 
       System.out.println(myLinkedList.binaryToDecimal());

 */


/*     myLinkedList.append(4); 
    myLinkedList.append(2);
    myLinkedList.append(5); */
    
    


  /*   myLinkedList.printList(); // Print the linked list
        System.out.println("\n");
    myLinkedList.removeDuplicates(); // Remove duplicates from the linked list
    myLinkedList.printList(); // Print the linked list after removing duplicates
 */
    /*1 -> 2 -> 3 -> 1 -> 4 -> 2 -> 5 
    myLinkedList.patitionList(5); // Partition the linked list around the value 5
    myLinkedList.printList(); // Print the linked list after partitioning

     */
    /* myLinkedList.reverse(); // Reverse the linked list

    myLinkedList.printList(); // Print the linked list after reversal
 */
/*     myLinkedList.append(3); // Append a new node with value 1 to the linked list
    myLinkedList.append(23); // Append a new node with value 2 to the linked list
    myLinkedList.append(7); // Append a new node with value 3 to the linked list
   
    myLinkedList.remove(2); // Remove the node at index 2 from the linked list
    myLinkedList.printList(); // Print the linked list */




/*     myLinkedList.append(2); // Append a new node with value 1 to the linked list

    myLinkedList.insert(1, 1); // Insert a new node with value 2 at index 1
        myLinkedList.printList(); // Print the linked list
 */

        /*      myLinkedList.append(1);
     myLinkedList.append(2); // Append a new node with value 1 to the linked list
     myLinkedList.append(3); // Append a new node with value 2 to the linked list
        myLinkedList.set(1, 4); // Set the value of the node at index 1 to 4

        myLinkedList.printList(); // Print the linked list
 */


    // System.err.println(myLinkedList.get(2).value+"\n"); // Get the node at index 2 and print its value
    // myLinkedList.printList(); // Print the linked list
    // myLinkedList.append(3); // Append a new node with value 5 to the linked list
    // myLinkedList.prepend(1);
    // myLinkedList.printList(); // Print the linked list
    // myLinkedList.removeFirst(); // Remove the first node from the linked list
    
    // myLinkedList.printList(); // Print the linked list
    // myLinkedList.removeFirst(); // Remove the first node from the linked list
    
    // myLinkedList.printList(); // Print the linked list
    // myLinkedList.removeFirst(); // Remove the first node from the linked list
    
    // myLinkedList.printList(); // Print the linked list

    //System.out.println(myLinkedList.removeLast().value); // Remove the last node from the linked list and print its value
        //System.out.println(myLinkedList.removeLast().value); // Remove the last node from the linked list and print its value
        //System.out.println(myLinkedList.removeLast()); // Remove the last node from the linked list and print its value
      }


}
